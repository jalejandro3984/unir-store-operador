server:
  port: 8001
  error:
    include-stacktrace: on_param

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_URL:http://localhost:8761/eureka}
  instance:
    prefer-ip-address: true
    hostname: ms-unir-store

spring:
  application:
    name: ms-unir-store
  datasource:
    url: jdbc:h2:mem:unir_store;Mode=MySQL
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create-drop
    defer-datasource-initialization: true
  h2:
    console:
      enabled: true
      path: /h2-console
      settings:
        web-allow-others: true

jasypt:
  encryptor:
    password: this-is-a-secret-key
#getProducts:
#  url: http://ms-unir-store-buscador/products/%s